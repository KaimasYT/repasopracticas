name: HelloGithub
on: 
    push:
        branches:
                - main

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout Repos
              uses: actions/checkout@v4

            - name: HelloWorld
              run: echo "Hello GitHub2"
